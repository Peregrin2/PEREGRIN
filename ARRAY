#include <iostream>
using namespace std;
int main(){
    /*
     int arr[5];
    int sum;

    cout<<"Enter Five Number: ";
    cin>>arr[0]>>arr[1]>>arr[2]>>arr[3]>>arr[4];

    sum = (arr[0] + arr[1] + arr[2] + arr[3] + arr[4]);

    cout<<"The Sum is: "<<sum;
    */

    /*
    int arr[5];
    int largest;
    cout<<"Enter Five Numbers: ";

    for(int i = 0; i < 5; i++){
        cin>>arr[i];
    }

    largest = arr[0];

    for(int i = 0; i < 5; i++){
        if(arr[i] > largest){
        largest = arr[i];
    }
    }

    cout<<"The Largest Number is: "<<largest;
    */
    
    /*
    int arr[5];
    int reverses;

    cout<<"Enter Five Number: ";
    cin>>arr[0]>>arr[1]>>arr[2]>>arr[3]>>arr[4];        //notsure if tama ba talaga
    for(int i = 0; i <= 4; i++){
        cout"Original: "<<arr[i]<<"\t";

    }
    cout<<endl;
    for(int i = 4; i >= 0; i--){
        reverses = arr[i];
        cout<<"Reversed: "<<reverses<<"\t";
    }
    */

    
    
    
return 0;
}
